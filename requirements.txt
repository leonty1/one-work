# 文档解析
mammoth>=1.6.0          # Word → Markdown
pymupdf>=1.24.0         # PDF 文本提取
python-pptx>=0.6.23     # PPT 处理

# Markdown 处理
markdown>=3.6.0
python-markdown-math>=0.8

# 输出渲染
python-docx>=1.1.2      # 生成 Word

# 可选依赖 - 增强文档解析能力 (推荐安装)
# markitdown>=0.0.1a3   # Microsoft 开源文档转 Markdown 工具
#                       # 支持: DOCX, PDF, PPTX, XLSX, 图片, 音频等
#                       # 安装: pip install markitdown

# 网页转 Markdown (推荐安装)
# trafilatura>=1.12.0   # 智能网页内容提取，自动过滤广告/导航
# markdownify>=0.13.0   # HTML 转 Markdown
# requests>=2.32.0      # HTTP 请求
# beautifulsoup4>=4.12  # HTML 解析
#                       # 安装: pip install trafilatura markdownify requests beautifulsoup4
#                       # 功能: onework.read_url(url) 将任意网页转为 Markdown

# weasyprint>=62.0      # PDF 生成（需系统依赖）
# pillow>=10.0.0        # 图片处理
# pytesseract>=0.3.10   # OCR 识别
